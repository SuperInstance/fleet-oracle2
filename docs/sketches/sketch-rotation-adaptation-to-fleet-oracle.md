# Sketch: Rotation Adaptation to Fleet Oracle

**Status:** GitHub-only (`SuperInstance/sketch-rotation-adaptation-to-fleet-oracle`). Single README.md.
**Date:** 2026-06-14

## Intent

Forgemaster pushed rotation crates (neon-kernel, pid-cascade, attractor, log-tensor, rotation-core). Adapted as 5th inference module in fleet-oracle v0.3.0. ARM-optimized for Neoverse-N1 with baseline normalization for singleton-fleet structural error.

## Extracted Note

One-shot status note about integrating rotation crates into fleet-oracle. The actual rotation crates live in their own repos (`neon-kernel`, `pid-cascade`, etc.) and the integration work was tracked elsewhere. This sketch is a completion marker.
