# Sketch: Workspace Sketchbook Pattern

**Status:** GitHub-only (`SuperInstance/sketch-workspace-sketchbook-pattern`). Single README.md.
**Date:** 2026-06-14

## Intent

Push ideas to GitHub repos, clear local workspace. `fleet-sketchbook` CLI tool. The workspace is a draft board, not an archive.

## Extracted Note

This is the meta-sketch describing the pattern used to create all the other sketch repos. The pattern is:
1. Draft idea in workspace
2. Push to GitHub as a sketch repo (single README.md)
3. Clear local workspace

This is the pattern that created the other sketch repos. It's well-documented in the repo itself. No local action needed — the repo itself is the documentation. The concept is now live in practice.
